name: 🤖🆙❄️

permissions:
  contents: write
  pull-requests: write

on:
  pull_request:
    paths:
      - '.github/workflows/bump-flake-lock-and-selfup.yml'
  schedule:
    # Every 10:17(JST) on Monday
    # https://docs.github.com/en/actions/using-workflows/events-that-trigger-workflows#schedule
    # You can understand the config actually say what in https://crontab.guru/
    - cron: '17 1 * * 1'
  workflow_dispatch:

jobs:
  bump:
    # NOTE: Since removed selfup depednecies in workflows, we can consider other action or https://github.blog/changelog/2026-04-07-dependabot-version-updates-now-support-the-nix-ecosystem/
    uses: kachick/selfup/.github/workflows/reusable-bump-flake-lock-and-selfup.yml@06a37ddb70ce25ec28b75ce500e15e7fb4d7c4a9 # v1.3.1
    with:
      app_id: ${{ vars.SELFUP_APP_ID }}
      dry-run: ${{ github.event_name == 'pull_request' }}
      pr-title: 'Bump flake.lock and related dependencies'
      optional-run: |
        echo 'Add another changes and git commit here, especially for .node-version/.ruby-version'
    secrets:
      APP_PRIVATE_KEY: ${{ secrets.SELFUP_APP_PRIVATE_KEY }}
